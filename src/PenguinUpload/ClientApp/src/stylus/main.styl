// main.styl
@import '../../node_modules/vuetify/src/stylus/settings/_colors'

$theme := {
  primary: $cyan.darken-2
  accent: $light-blue.accent-3
  secondary: $grey.darken-3
  info: $blue.base
  warning: $amber.base
  error: $red.base
  success: $green.base
}

@import '../../node_modules/vuetify/src/stylus/main'

/* Overrides */

h2, h3, h4, h5, h6 {
  font-weight: 300;
}